name: ci

# Version policy: action majors (checkout@v5, setup-go@v5, setup-node@v4,
# golangci-lint-action@v8) are the latest known-good as of plan writing —
# bump to the current majors when touching this file if newer exist.

on:
  push:
    branches: [main]
  pull_request:

jobs:
  go:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/api
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.5"
          cache-dependency-path: apps/api/go.sum
      - name: Lint (zero warnings policy)
        uses: golangci/golangci-lint-action@v8
        with:
          version: v2.12.2
          working-directory: apps/api
      - name: Tests (includes testcontainers integration)
        run: go test ./...
      - name: govulncheck (version policy gate)
        run: |
          go install golang.org/x/vuln/cmd/govulncheck@latest
          govulncheck ./...

  codegen-drift:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v5
        with:
          go-version: "1.26.5"
          cache-dependency-path: apps/api/go.sum
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: npm
      - name: Install workspace deps
        run: npm ci
      - name: npm audit (version policy gate)
        run: npm audit --audit-level=high
      - name: sqlc drift check
        run: |
          go install github.com/sqlc-dev/sqlc/cmd/sqlc@v1.31.1
          sqlc generate
          git diff --exit-code
      - name: OpenAPI + orval drift check
        run: |
          make openapi
          npm run generate -w packages/api-client
          git diff --exit-code

  docker:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5
      - name: Build API image
        run: docker build -f deploy/Dockerfile.api .
